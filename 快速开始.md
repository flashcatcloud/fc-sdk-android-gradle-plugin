# Flashcat Android Gradle Plugin 快速开始

## 📦 插件介绍

这是一个用于上传 Proguard/R8 混淆映射文件和 NDK 符号文件到 Flashcat 平台的 Gradle 插件。

**基于**: Datadog dd-sdk-android-gradle-plugin (Apache 2.0)  
**适配**: Flashcat 平台

---

## 🚀 快速开始

### 1. 在 build.gradle 中引入插件

```groovy
plugins {
    id("cloud.flashcat.android-gradle-plugin") version "1.1.0"
}
```

或在 Kotlin DSL 中：

```kotlin
plugins {
    id("cloud.flashcat.android-gradle-plugin") version "1.1.0"
}
```

### 2. 配置插件

```groovy
flashcat {
    site = "CN"                    // Flashcat 站点：CN 或 STAGING
    sourcemapEndpoint = "https://rum.example.com" // 可选，私有化 sourcemap 上传地址；未带 /sourcemap/upload 时会自动补全
    versionName = "1.3.0"          // 可选，默认读取 Android 配置
    serviceName = "my-app"         // 可选，默认使用包名
}
```

### 3. 配置 API Key

**方式 1：环境变量（推荐）**

```bash
export FC_API_KEY="your-flashcat-api-key"
export FLASHCAT_SOURCEMAP_INTAKE_URL="https://rum.example.com" # 可选，私有化 sourcemap 上传地址
```

**方式 2：Gradle 属性 (gradle.properties)**

```properties
FC_API_KEY=your-flashcat-api-key
```

**方式 3：flashcat-ci.json 文件**

```json
{
  "apiKey": "your-flashcat-api-key",
  "flashcatSite": "ci.flashcat.cloud",
  "sourcemapEndpoint": "https://rum.example.com"
}
```

### 4. 上传文件

```bash
# 上传混淆映射文件
./gradlew uploadMappingRelease

# 上传 NDK 符号文件
./gradlew uploadNdkSymbolFilesRelease
```

---

## 📋 完整配置选项

```groovy
flashcat {
    // 必填
    site = "CN"  // 或 "STAGING"

    // 可选配置
    sourcemapEndpoint = "https://rum.example.com"
    versionName = "1.3.0"
    serviceName = "my-service"
    checkProjectDependencies = "warn"  // "warn", "fail" 或 "none"
    mappingFilePath = "path/to/mapping.txt"
    nonDefaultObfuscation = false
    ignoreFlashcatCiFileConfig = false
    additionalSymbolFilesLocations = ["/path/to/location/obj"]
}
```

---

## 🌐 可用站点

| 站点    | 地址                  | 说明         |
| ------- | --------------------- | ------------ |
| CN      | ci.flashcat.cloud     | 生产环境     |
| STAGING | ci-dev.flashcat.cloud | 内部测试环境 |

---

## 🔑 API Key 优先级

配置优先级从高到低：

1. Gradle 属性 (`FC_API_KEY` / `FLASHCAT_API_KEY`)
2. 环境变量 (`FC_API_KEY` / `FLASHCAT_API_KEY`)
3. flashcat-ci.json 文件中的 `apiKey`

---

## 🎯 Variant 配置

如果需要为不同的 variant 配置不同的参数：

```groovy
flashcat {
    site = "CN"  // 默认配置
    sourcemapEndpoint = "https://rum.example.com" // 默认私有化上传地址

    variants {
        production {
            site = "CN"
            serviceName = "my-app-prod"
        }
        staging {
            site = "STAGING"
            sourcemapEndpoint = "https://rum-staging.example.com/sourcemap/upload"
            serviceName = "my-app-staging"
        }
    }
}
```

---

## ⚙️ Gradle 配置缓存支持

如需使用 Gradle 配置缓存，请禁用依赖检查：

```groovy
flashcat {
    checkProjectDependencies = "none"
}
```

---

## 🐛 故障排查

### 问题：找不到 API Key

**错误消息**:

```
Make sure you define an API KEY to upload your mapping files to Flashcat.
Create a FC_API_KEY or FLASHCAT_API_KEY environment variable...
```

**解决方案**:

- 检查环境变量是否正确设置
- 检查 gradle.properties 文件
- 检查 flashcat-ci.json 文件是否存在且格式正确

### 问题：API Key 格式错误

**错误消息**:

```
FC_API_KEY provided shouldn't contain quotes or apostrophes.
```

**解决方案**:

- 移除 API Key 中的引号
- 确保环境变量值不包含 `"` 或 `'`

---

## 📞 技术支持

遇到问题？请联系 Flashcat 技术支持。

---

## 📄 协议说明

本项目基于 Datadog 的 dd-sdk-android-gradle-plugin 进行修改和适配。

- **原始项目**: https://github.com/DataDog/dd-sdk-android-gradle-plugin
- **协议**: Apache License 2.0
- **版权**: 原始作品 © 2019-2024 Datadog, Inc. | 修改部分 © 2025 Flashcat, Inc.

详见 [FORK_NOTICE.md](FORK_NOTICE.md) 和 [LICENSE](LICENSE)。
